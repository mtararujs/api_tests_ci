pipeline {
    agent any
    triggers {
        pollSCM('0 * * ? * * *')
    }
    stages {
        stage('build') {
            steps {
                sh "docker build -t api_test_base . -f Dockerfile_base"
            }
        }
    }
}